import { PsSuspensivoPipe } from './ps-suspensivo.pipe';

describe('PsSuspensivoPipe', () => {
  it('create an instance', () => {
    const pipe = new PsSuspensivoPipe();
    expect(pipe).toBeTruthy();
  });
});
